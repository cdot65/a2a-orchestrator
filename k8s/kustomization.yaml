apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

namespace: a2a

resources:
  - namespace.yaml
  - deployment-recipe-url.yaml
  - deployment-recipe-gen.yaml
  - deployment-orchestrator.yaml
  - ingressroute.yaml
  # Secret is created manually — do NOT list it here.
